📋 Задания к лаб. 11
Заполнение массива с консоли

Преобразование массива

Печать массива

Найти сумму элементов после элемента с минимальным абсолютным значением

📋 Задания к лаб. 14
Заполнение массива значениями из функции f(x) = x² + 2π·cos(x)

Печать массива

Вычисление суммы элементов массива в заданном диапазоне

Поиск элемента в массиве

Округление элементов вверх с помощью ceil()

Поиск максимального значения на четных/нечетных позициях

📋 Задания к лаб. 16/17
Реализация функций сортировки массива:

пузырьковой сортировки

шейкерной (коктейльной) сортировки

сортировки выбором

сортировки вставками

Проведение эксперимента, сравнивающего скорость выполнения каждой из сортировок

Генерация случайных массивов для тестирования

📁 Структура проекта
text
laboratory-work-16/
├── main.c              # Главный файл с полной реализацией
└── README.md           # Документация
🔧 Реализованные функции
1. void prog1()
Назначение: Вычисление суммы элементов после элемента с минимальным абсолютным значением

Алгоритм:

Ввод размера массива и элементов

Поиск индекса элемента с минимальным модулем

Вычисление суммы всех элементов после найденного

Возвращает: ничего (результат выводится на экран)

2. void prog2()
Назначение: Работа с массивом вещественных чисел (лабораторная 14)

Параметры: нет

Возвращает: ничего

Реализованные операции:

Заполнение массива значениями функции f(x) = x² + 2π·cos(x)

Вычисление суммы элементов в указанном диапазоне

Поиск элемента по значению

Округление элементов вверх

Поиск максимума на четных/нечетных позициях

3. void fill_random(int* a, int n)
Назначение: Заполнение массива случайными целыми числами

Параметры:

a - указатель на массив int

n - количество элементов

Диапазон значений: [-500, 499]

Возвращает: ничего

4. void bubble(int* a, int n)
Назначение: Пузырьковая сортировка по возрастанию

Параметры:

a - указатель на массив int

n - количество элементов

Алгоритм: последовательное сравнение соседних элементов

Возвращает: ничего

5. void shaker(int* a, int n)
Назначение: Шейкерная (коктейльная) сортировка по возрастанию

Параметры:

a - указатель на массив int

n - количество элементов

Особенность: проходит в обоих направлениях

Возвращает: ничего

6. void selection(int* a, int n)
Назначение: Сортировка выбором по возрастанию

Параметры:

a - указатель на массив int

n - количество элементов

Алгоритм: поиск минимального элемента и обмен с текущей позицией

Возвращает: ничего

7. void insertion(int* a, int n)
Назначение: Сортировка вставками по возрастанию

Параметры:

a - указатель на массив int

n - количество элементов

Алгоритм: последовательная вставка элементов в отсортированную часть

Возвращает: ничего

8. void prog3()
Назначение: Сравнение алгоритмов сортировки (лабораторная 16/17)

Параметры: нет

Возвращает: ничего

Функциональность:

Генерация случайного массива

Выбор алгоритма сортировки через меню

Замер времени выполнения

Тестирование всех четырех алгоритмов

Вывод первых 20 элементов до и после сортировки

9. void put_array_d(double* a, int n)
Назначение: Печать массива вещественных чисел

Параметры:

a - указатель на массив double

n - количество элементов для вывода

Формат вывода: числа с двумя знаками после запятой

Возвращает: ничего

10. void put_array_i(int* a, int n)
Назначение: Печать массива целых чисел

Параметры:

a - указатель на массив int

n - количество элементов для вывода

Возвращает: ничего
